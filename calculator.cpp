#include "calculator.h"

// 필요하다면 함수 추가가능
// 파라미터도 임의로 필요할거 같은거 넣어둔거라 필요하면 추가하거나 빼는거 가능


void Calculator::isRoundOver(std::vector<Player*> playerList) {};    // 한 라운드마다 승리인지 판별하는 함수
void Calculator::calculateScore(std::vector<Player*> playerList, int goCount) {}; // 점수를 계산하는 함수